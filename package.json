{
  "name": "butter-dog-trash-pet",
  "version": "0.1.0",
  "description": "A Butter Dog desktop pet that eats dropped files and moves them to the OS trash.",
  "main": "src/main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/renderer.js && node scripts/smoke-test.js",
    "package:win": "electron-packager . ButterDogTrash --platform=win32 --arch=x64 --out=dist --overwrite",
    "package:mac": "electron-packager . ButterDogTrash --platform=darwin --arch=x64 --out=dist --overwrite",
    "package:mac:arm64": "electron-packager . ButterDogTrash --platform=darwin --arch=arm64 --out=dist --overwrite"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.6",
    "electron": "^43.1.0"
  }
}
